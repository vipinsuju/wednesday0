import React from 'react'
import CardContainer from '../Container/CardContainer'

const Card = ({prod}) => {
    return (
        <div>
            <CardContainer prod={prod} />
        </div>
    )
}

export default Card
